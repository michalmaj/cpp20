#include <iostream>

int main()
{
	int condition{ -3 };

	// If 0 then false otherwise true
	if (condition)
		std::cout << "We have a " << condition << " in our variable\n";
	else
		std::cout << "We have a " << condition << " in our variable\n";

	return 0;
}