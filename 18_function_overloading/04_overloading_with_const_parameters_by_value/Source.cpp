#include <iostream>


// Those functions are equivalent - so we have a redefinition! We need to comment out one of them
int max(const int a, const int b)
{
    return (a > b)? a : b;
}

//int max(int a, int b)
//{
//    ++a;
//    return (a > b) ? a : b;
//}

int main() {


    return 0;
}

